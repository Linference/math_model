---
name: math-modeling
description: 端到端数学建模竞赛论文生成系统（国赛 CUMCM 中文 / 美赛 MCM 英文）。多智能体对抗协作：深度审题→建模框架与方法选型(含是否用ML/DL)→联网找数据(维基/GitHub/Kaggle/官方统计/sklearn)→编程求解与可视化→LaTeX写作→编译PDF→写作者/审稿人/验证者/推理者多轮对抗审稿(评分5.0→7.5)，产出国赛级别论文。当用户给出一道数学建模赛题、或说"数学建模""建模论文""国赛/美赛论文""/math-modeling"时使用。
---

# 数学建模多智能体系统

把一道赛题，经 7 个阶段、多智能体对抗协作，产出可编译为 PDF 的国赛级别论文。

## 环境（本机 Windows，已验证）
- 编译：`xelatex`(MiKTeX) + `ctex`/`xeCJK`，中文字体 SimHei/SimSun → 中文 PDF 可编。
- Python：anaconda `python`（numpy/pandas/scipy/sklearn/matplotlib 全就绪）。命令行显式用 `python`。
- 联网：内置 WebSearch / WebFetch。
- Skill 根目录：`C:/Users/HUAWEI/.claude/skills/math-modeling/`（下称 `<skill>`）。

## 何时用哪种执行方式
- **子智能体**（`Agent` 工具，agentType 见下）：单阶段专项工作。
- **对抗审稿**：用 `Workflow` 跑 `<skill>/workflows/adversarial-review.js`（真实多智能体并行对抗，≤4 轮）。
- 只有当用户明确要"多智能体/工作流/对抗"或说了触发词时才启动 Workflow（它会开多个子智能体，耗 token）。

---

## ⛔ 强制流程门禁（不允许跳步）

**这是硬性约束，优先级高于一切效率考量。必须严格按阶段 0→1→2→3→4→5→6→7 顺序执行，禁止跳过、合并、颠倒任何阶段。**

### 核心规则

1. **每个阶段三步骤**：执行 → 验证 → 确认。验证不通过不得进入下一阶段。
2. **落盘即证据**：每个阶段的产出必须写入对应文件，产出不存在 = 该阶段未完成。
3. **禁止心算跳过**：不允许因为"这题简单""数据不用找""图随便画"而略过任何阶段。
4. **阶段门禁清单**：开工时用 `TaskCreate` 建 8 个任务（阶段 0–7），每进入一个阶段 `TaskUpdate` 设为 in_progress，验证通过后才设 completed。任何时刻只允许一个阶段是 in_progress。
5. **审题结论贯穿全流程**：阶段 1 的审题报告是后续所有阶段的"宪法"——阶段 2 选型必须引用审题结论，阶段 4 求解必须回应审题中的约束，阶段 6 写作必须覆盖审题中的每问和隐性要求。
6. **可视化是必经阶段**：阶段 5 必须为每个模型至少产出 1 张图到 `figures/`，且阶段 6 的论文里每张图都要被正文引用并解读。
7. **对抗审稿不可省**：阶段 7 是核心创新，除非用户明确说"不要审稿"，否则必须跑。

### 阶段依赖链

```
0 目录 → 1 审题报告 → 2 建模方案表 → 3 data/*.csv+SOURCES
  → 4 code/solve_qN.py+结果 → 5 figures/*.png → 6 main.pdf
  → 7 评分记录+终版PDF
```

### ⛔ 每阶段验证门禁（新增 — 核心架构）

**每个阶段完成后，必须执行以下验证步骤，验证不通过则退回该阶段重做。**

#### 阶段 1 验证：审题完整性

| 验证项 | 方法 | 不通过处理 |
|---|---|---|
| 每小问逐一拆解 | 核对 `problem.md` 问数与 REPORT.md §1 拆解数一致 | 补拆缺失小问 |
| 隐性约束全部识别 | 逐条列出：数据来源约束、假设边界、输出格式要求 | 补识别遗漏约束 |
| 评分点与陷阱标注 | 每条陷阱必须有"如果踩了会怎样"的具体后果 | 补陷阱分析 |
| **问题类型判定** | 判定题目类型（优化/评价/预测/机理/混合），存入 REPORT.md §1.7 | 类型判定错误会导致后续方法选偏 |
| 审题报告 ≥ 2000 字 | 检查 REPORT.md §1 字数 | 展开细节，不可浓缩 |
| **审题结论是否可被后续阶段引用** | 检查 §1.7 问题类型判定是否明确（优化/评价/预测/机理/混合），§1.2 逐问拆解是否每问独立可引用 | 补全 §1.7 或 §1.2 |

#### 阶段 2 验证：方法选型合理性

| 验证项 | 方法 | 不通过处理 |
|---|---|---|
| 每问有对应方法 | 核对问数 = 方案表行数 | 补遗漏问题 |
| ML/DL 决策有理由 | 检查"是否用 ML/DL"列，每行必须有具体理由（非"不需要"三个字） | 补理由 |
| 方法匹配问题类型 | 对照 §1.7 的问题类型，检查方法是否偏离 | 如优化题选了纯评价方法→重选 |
| 图表清单完整 | 每行至少 2 张图 | 补图 |
| 验证方式可行 | 每行验证列不是"分析"等空话 | 补具体验证方案 |

#### 阶段 3 验证：数据质量

| 验证项 | 方法 | 不通过处理 |
|---|---|---|
| CSV 可读且非空 | `python -c "import pandas as pd; df=pd.read_csv(...); print(df.shape)"` | 重新获取 |
| SOURCES.md 记录来源 | 每个 CSV 文件至少一条来源记录，含 URL | 补来源 |
| 数据量 ≥ 建模需求 | 样本数 ≥ 指标数 × 3（基本统计要求） | 补充数据或标注限制 |
| 模拟数据已标注 | 若数据为生成/模拟，必须在 SOURCES.md 显式标注"模拟" | 补标注 |

#### 阶段 4 验证：算法正确性（⛔ 最关键门禁）

**这是整个流水线最容易出问题的环节。必须逐项通过，否则所有后续工作建立在错误基础上。**

| 验证项 | 方法 | 不通过处理 |
|---|---|---|
| **代码可运行** | 在终端执行 `python code/solve_qN.py`，确认无报错 | 修 bug |
| **输出有具体数值** | 检查 stdout/结果文件含具体数字（非 "最优解是 x" 这种占位符） | 确保真实跑通 |
| **边界条件测试** | 用极端参数（0、极大值、负值）跑一次，不崩溃 | 加固代码 |
| **量纲一致性** | 检查所有输出数字带单位，且单位体系统一（如全用 kgCO2e 或全用 tCO2e） | 统一单位 |
| **公式一致性审计** | 对照阶段 2 建模方案表中的公式，检查代码实现是否一致（变量名、参数含义、正负号） | 修正不一致 |
| **随机种子固定** | 所有随机代码含 `np.random.seed(N)` | 补种子 |
| **关键数字可追溯** | 论文中将引用的每个关键数字，必须在代码输出中能找到对应来源（不可凭空出现） | 确保每个数字有出处 |

**阶段 4 验证通过后，必须把验证结果写入 REPORT.md §4 末尾**：
```
### 4.N 阶段 4 验证记录
- 代码可运行: ✅ solve_q1.py / solve_q2.py / ... 全部跑通
- 边界测试: ✅ 极端参数不崩溃
- 量纲一致性: ✅ 全用 kgCO2e
- 公式一致性: ✅ 代码与方案表公式一致
- 验证人: mm-verifier
```

#### 阶段 5 验证：图表覆盖

| 验证项 | 方法 | 不通过处理 |
|---|---|---|
| 每模型 ≥ 1 张图 | 统计 `figures/` 下 PNG 数 ≥ REPORT.md §2 模型数 | 补图 |
| 每图 300dpi | 抽查 1-2 张图属性 | 重出 |
| 每图有解读文案 | 检查 REPORT.md §5 每张图有 1-2 句解读 | 补解读 |

#### 阶段 6 验证：论文质量

| 验证项 | 方法 | 不通过处理 |
|---|---|---|
| **页面约束（摘要 1 页）** | 打开 PDF 检查 | 压缩或补内容 |
| **页面约束（目录 1 页）** | 打开 PDF 检查 | 调 tocdepth |
| **无大空白/溢出** | 逐页翻 PDF | 修浮动体 |
| **每图被 \ref 引用** | 搜索 `\ref{fig:` 在 main.tex 中的出现次数，对比 figures/ PNG 数量 | 补引用 |
| **摘要含量化数字** | 人工检查摘要段落中数字出现次数，应 ≥ 4 个 | 补量化结论 |
| **四项深度要求** | 逐项核对：假设深度/对比模型/创新声明/多指标 | 补内容 |
| **代码输出与论文数字交叉验证** | 运行 `python <skill>/scripts/verify_results.py <slug>` | 修正不一致数字 |

#### 阶段 7 验证：审稿闭环

| 验证项 | 方法 | 不通过处理 |
|---|---|---|
| 每轮评分记录在案 | 检查 REPORT.md §6 有评分表 | 补记录 |
| 终版 PDF 已编译 | `paper/main.pdf` 存在且更新时间 > 审稿开始时间 | 重新编译 |
| 版本 PDF 齐全 | `paper/versions/` 至少含基线 + 每轮修改 + 终版 | 补编译 |
| High 级别弱点清零或降级 | 终版残留 high 弱点 ≤ 1 个 | 继续修改 |

---

## 可用子智能体（已装于 ~/.claude/agents）
| agentType | 角色 | 阶段 |
|---|---|---|
| mm-problem-analyst | 审题 | 1 |
| mm-modeler | 方法选型(含ML/DL决策) | 2 |
| mm-data-hunter | 联网找数据 | 3 |
| mm-coder | 编程求解+可视化 | 4-5 |
| mm-writer | 论文写作/修改 | 6,7 |
| mm-reviewer | 批判审稿 | 7 |
| mm-verifier | 交叉验证 | 4,5,6,7 (每阶段参与) |
| mm-reasoner | 数学推理/证明 | 2,4,7 |

---

## 执行流程（7 阶段，每阶段含验证）

### 阶段 0 — 建立工作目录
先问/推断赛制语言：国赛→`zh`，美赛→`en`。然后：
```bash
python <skill>/scripts/new_project.py "<赛题名>" --lang zh   # 或 en
```
把赛题原文粘进 `<slug>/problem.md`。之后所有产出落在该工作目录。
用 `TaskCreate` 建立阶段 0–7 共 8 个门禁任务。

> ✅ 门禁：工作目录+8 个任务已建、`problem.md` 已填 → 验证通过 → 进入阶段 1。

---

### 阶段 1 — 深度审题（决定一切的方向盘）

用 `Agent`（agentType=`mm-problem-analyst`）读 `problem.md`，产出结构化审题报告。

**⛔ 审题深度要求——这是全流程的"宪法"，不允许浓缩：**

1. **逐问拆解**（每小问独立一段）：
   - 输入是什么？输出是什么？约束条件逐条列出
   - 问与问之间的依赖关系（画依赖图）
   - 每问的隐性要求（题目没说但评分会扣的）

2. **显性约束清单**：从题目原文逐句提取，不得遗漏

3. **隐性约束推导**（最重要！每条必须给出推导依据）：
   - 从"现实合理性"推导的约束
   - 从"数据可获得性"推导的约束
   - 从"模型可求解"推导的约束
   - 从"评分标准暗示"推导的约束

4. **数据情况评估**：
   - 题目直接给什么数据？格式？量级？
   - 缺什么数据？可能从哪获取？
   - 数据质量风险预判

5. **评分点与陷阱**：
   - 每条陷阱标注"如果踩了会怎样"的具体后果
   - 例如："如果漏掉 Scope 3 排放 → 碳足迹低估 50%+ → 结论不可信 → 扣 15-20 分"

6. **问题间依赖关系图**：ASCII 图，标注哪些问可以独立求解、哪些有先后

7. **⛔ 问题类型判定**（驱动后续阶段策略）：
   - 判定为以下之一：**优化类 / 评价类 / 预测类 / 机理类 / 混合类**
   - 给出判定依据（至少 3 条）
   - 该类型对应的建模策略要点
   - 该类型最容易犯的错误

写入 `REPORT.md` 第 1 节。审题报告不得少于 **2000 字**。

> ⛔ 阶段 1 验证（6 项全部通过才可进入阶段 2）：参见上方"阶段 1 验证"表。
> 参考：`01-problem-analysis.md`。

---

### 阶段 2 — 建模框架与方法选型

用 `Agent`（`mm-modeler`）基于阶段 1 审题报告（必须引用 §1.7 问题类型），为每问选方法。

**⛔ 方法选型必须回应审题结论**：

1. **问题类型驱动方法选择**：
   - 优化类 → 优先考虑 LP/IP/NLP/启发式
   - 评价类 → 优先考虑 AHP/TOPSIS/熵权/灰色关联
   - 预测类 → 优先考虑时序/回归/ML（考虑样本量）
   - 机理类 → 优先考虑微分方程/系统动力学/元胞自动机
   - 混合类 → 划分问题子结构，分别选型

2. **ML/DL 决策**（每问必判，给出具体理由，不能用"不需要"三个字敷衍）：
   - 样本量是否 ≥ 100？
   - 是否有可学习的模式 vs 确定性关系？
   - 可解释性要求是否高于预测精度？
   - 选择/不选 ML/DL 的具体代价是什么？

3. **每问方案表**：方法 | 输入 | 输出 | ML/DL 判定+理由 | 必备图 | 验证方式

4. **⛔ 算法可行性预审**（新增——在选型阶段就排除不可行方案）：
   - 每个方法标注计算复杂度
   - 标注收敛性条件（优化类）/ 适用条件（统计类）
   - 标注已知坑点（如 AHP 的 CR 检验、TOPSIS 的逆序问题）
   - 如果方法对数据有要求（如正态性），标注检验方法

写入 `REPORT.md` 第 2 节。

> ⛔ 阶段 2 验证（5 项全部通过才可进入阶段 3）：参见上方"阶段 2 验证"表。
> 参考：`02-framework.md`、`04-modeling-cookbook.md`。

---

### 阶段 3 — 数据获取

用 `Agent`（`mm-data-hunter`，带 WebSearch/WebFetch）按方案找数据，用 `<skill>/scripts/fetch_data.py` 落盘到 `data/`，来源记入 `data/SOURCES.md`。

> ⛔ 阶段 3 验证（4 项全部通过才可进入阶段 4）：参见上方"阶段 3 验证"表。
> 参考：`03-data-acquisition.md`、`data-sources.md`。

---

### 阶段 4 — 建模求解 + 编程（⛔ 最关键阶段）

用 `Agent`（`mm-coder`）用 anaconda `python` 实现模型、跑通、存结果。

**⛔ 编码前必须做的事（算法设计审核）**：

在写代码之前，mm-coder 必须先输出算法设计文档（写入 REPORT.md §4 开头）：

1. **算法选择**：对于每个模型，列出 2-3 种可选算法，比较后选择
2. **算法正确性论证**：
   - 该算法为什么适用于本问题？（引用阶段 2 的方案表）
   - 收敛性/最优性保证条件是什么？
   - 已知的失效模式是什么？怎么规避？
3. **数值稳定性预案**：矩阵奇异、除零、梯度消失等情况的处理
4. **关键参数取值依据**：学习率、种群大小、正则化系数等——不能"凭经验"

**编码规范**：
- 每问 `code/solve_qN.py`，固定随机种子 `np.random.seed(42)`
- 代码顶部 docstring 注明：求解的问题、使用的算法、关键公式编号
- 所有输出带单位
- 关键中间变量打印到 stdout（方便验证）

**⛔ 编码后必须执行算法验证（用 mm-verifier）**：

```bash
# 自动交叉验证：比对代码输出与论文中引用的数字
python <skill>/scripts/verify_results.py <slug> --stage 4
```

验证内容：
- 每个关键输出数字是否在代码中有明确来源（变量名+行号）
- 公式代码实现是否与阶段 2 方案表一致（变量名、参数、正负号）
- 边界条件测试：极端参数不崩溃
- 量纲一致性：所有数字单位统一

验证结果写入 REPORT.md §4 末尾。

> ⛔ 阶段 4 验证（7 项全部通过才可进入阶段 5）：参见上方"阶段 4 验证"表。
> ⛔ 这是整个流水线最关键的门禁——算法错误会污染所有后续阶段。

---

### 阶段 5 — 可视化（必经，不可略过）

仍由 `mm-coder`，用 `<skill>/scripts/plot_helpers.py` 出图到 `figures/`。

**硬性要求：**
- 每个模型至少 1 张说明性图；国赛级论文通常 6–12 张
- 基础必备图 + 高端图（按问题类型选 1-3 张）：
  - **优化类** → 收敛曲线 + 3D曲面/等高线
  - **评价类** → 雷达图 + 热力图 + 瀑布图
  - **预测类** → 拟合对比 + 残差图 + 小提琴图
  - **机理类** → 时序曲线 + 相图 + 山脊图
  - **混合类** → 组合上述
- 每张图规范：标题、轴标签+单位、图例、来源/随机种子（仿真图）；300dpi PNG
- 出图后写一句该图说明了什么，供阶段 6 写进正文

> ⛔ 阶段 5 验证（3 项全部通过才可进入阶段 6）：参见上方"阶段 5 验证"表。
> 参考：`05-visualization.md`。

---

### 阶段 6 — LaTeX 写作 + 编译

用 `Agent`（`mm-writer`）把审题/模型/结果/图表填进 `paper/main.tex`。

**⛔ 写作必须严格遵循审题报告（阶段 1）的框架**：
- 审题报告中的每小问拆解 → 对应论文中的独立小节
- 审题报告中的隐性约束 → 在论文中显式说明如何处理
- 审题报告中的陷阱 → 在论文中注释如何规避

**⛔ 页面硬约束**：摘要一页内、目录一页内、无大空白、无文字溢出、每节后 `\FloatBarrier`。

**⛔ 四项深度内容要求**：
1. 假设局限性深度讨论（每条含依据+放宽后果量化+补救方案）
2. 对比模型验证（≥2 基准/消融，≥3 指标，含对比表+雷达图）
3. 创新点显式声明（≥1 理论创新 + ≥1 方法创新）
4. 多指标评估（精度+效率+稳健性，冲突用 Pareto 前沿）

```bash
python <skill>/scripts/compile.py paper/main.tex
```

编译后运行交叉验证：
```bash
python <skill>/scripts/verify_results.py <slug> --stage 6
```

> ⛔ 阶段 6 验证（7 项全部通过才可进入阶段 7）：参见上方"阶段 6 验证"表。
> 参考：`06-writing.md`。

---

### 阶段 7 — 多轮对抗审稿（核心创新）

用 `Workflow` 运行对抗审稿：

```
Workflow({ scriptPath: "<skill>/workflows/adversarial-review.js",
           args: { draftPath: "<slug>/paper/main.tex", lang: "zh",
                   targetScore: 7.5, maxRounds: 4,
                   projectRoot: "<skill>",
                   dataContext: "<关键结果数值摘要>" } })
```

机制：写作者生成 → 审稿人/验证者/推理者**并行对抗打分**（0-10 五维度）→ 若 <7.5，写作者按弱点逐条修改（含**自动补充实验**）→ **每轮修改后自动编译并留存版本 PDF** → 复评，≤4 轮。

**⛔ 阶段 7 验证（4 项全部通过）：参见上方"阶段 7 验证"表。**
特别注意：终版 High 级别弱点必须 ≤ 1 个。

参考：`07-adversarial-review.md`、`scoring-rubric.md`、`08-stage-verification.md`。

---

## 交付物
- `<slug>/paper/main.pdf` —— 终版论文
- `<slug>/paper/versions/` —— **全部 PDF 版本**
- `<slug>/paper/main.tex`、`code/`、`figures/`、`data/`、`REPORT.md`（全过程可追溯）
- `<slug>/REPORT.md` 含每阶段验证记录

## 原则
- **稳中求新**：先保证基础分（模型合理、结果正确、排版规范），再加创新点。
- **不造假**：数据、结果、引用真实；模拟数据必须显著标注。
- **可复现**：代码固定随机种子、路径相对化，能放进附录复跑。
- **量化摘要**：摘要必须有具体数字，这是评分命脉。
- **排版即分数**：摘要/目录各一页、无大空白、无溢出。
- **对比即说服力**：每个模型必须有对比基准。
- **深度即区分度**：假设讨论到"放宽→误差X%→结论逆转"的粒度。
- **版本即安全网**：每次编译自动留存 PDF 版本。
- **⛔ 算法即根基**：阶段 4 是整条流水线的根基——算法错则全盘错。阶段 4 验证不通过，绝不允许进入可视化或写作。
- **⛔ 审题即宪法**：阶段 1 的审题结论是后续所有阶段的最高指导。阶段 2-6 必须显式引用审题报告中的问题类型、约束、陷阱。
